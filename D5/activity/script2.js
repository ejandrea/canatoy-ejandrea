const startingRange = parseInt(prompt("Enter starting range: "));

const endingRange = parseInt(prompt("Enter ending range: "));

for (let i = startingRange; i <= endingRange; i++) {
  console.log(i);
}
